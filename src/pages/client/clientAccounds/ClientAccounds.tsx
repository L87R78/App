import { Box } from '@/components/ui';
import classes from './styles';

const ClientAccounts = () => {
  return <Box sx={classes.layout}>Client accounts page</Box>;
};

export default ClientAccounts;
